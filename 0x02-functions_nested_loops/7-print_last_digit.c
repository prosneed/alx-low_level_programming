#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 * @int: passes the int number
 * Return: return the remainder always 1
 */

int print_last_digit(int)
{
	int x;

	return (x % 10);

}
