#include "main.h"
/**
 * int _isupper - checks if uppercase or not
 * @c - accepts c as a parameter
 * Return: 1 if upper or 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 65 && c <=96)
	{
		return (10);
	}
	else
	{
		return (0);
	}

}
